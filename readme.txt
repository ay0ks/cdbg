# clog : Simple logging and debugging utilities in C
------
 * Assertions always expand, ignoring `NDEBUG` or `DEBUG`.

### Reference:
 * Assert: `assert`, `assert_soft`, `goto_assert`
 * Dump: `dump`
